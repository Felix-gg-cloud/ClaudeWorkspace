package com.eqa.repository;

import com.eqa.entity.BossAttempt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BossAttemptRepository extends JpaRepository<BossAttempt, Long> {
}
